[project]
name = "webpage"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "python-dotenv",
    "bs4",
    "lxml",
    "tqdm",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
